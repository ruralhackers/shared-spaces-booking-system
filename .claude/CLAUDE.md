# Claude Code — Project-Specific Instructions

These instructions extend `AGENTS.md` (shared with Copilot) with behavior specific to Claude Code.

## Proactive Review Agents

After implementing or modifying production code, proactively run `/task-code-review`, `/task-tests-review`, and `/task-architecture-review` in parallel before reporting completion.

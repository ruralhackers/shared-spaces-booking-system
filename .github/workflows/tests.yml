name: Run Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.13"

      - name: Install dependencies
        run: bun install

      - name: Generate Prisma Client
        run: bun run --filter @dfs/database generate

      - name: Run tests
        run: bun test
        env:
          # Environment variables
          NODE_ENV: test
          WALLETS_API_ALLOWED_IPS: "127.0.0.1"

          # Secrets
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
